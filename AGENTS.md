# AGENTS.md

`yoshitaku-jp.github.io`（Astro 製のポートフォリオサイト）を触るときの規約。
**このファイルの責務は「デザイン規約と、破りやすい実装ルール」を1か所に置くこと。**
セットアップ手順は README.md を見る。

---

## 1. デザインの芯：和モダン

方針は「**藍 × 明朝 × 余白**」。日本らしさを柄や絵文字で出さず、**配色・組版・間**で出す。

### 配色（`src/layouts/BaseLayout.astro` の `:root`）

| トークン | 値 | 用途 |
| --- | --- | --- |
| `--washi` | `#f7f4ed` | 地（生成りの紙） |
| `--washi-sink` | `#efeae1` | 一段沈めた面 |
| `--ai` | `#1b3552` | 本文・見出し（藍） |
| `--ai-soft` | `#5a6e85` | 補足・ラベル |
| `--shu` | `#c1432b` | 差し色（朱） |
| `--line` | `#ded6c8` | 罫線 |

- **朱は差し色**。肩書き・停留点・現在地表示など、1画面で数箇所までに留める。広い面には塗らない。
- 新しい色を足さない。足すならこの表を更新する。

### 書体

| トークン | 用途 |
| --- | --- |
| `--font-mincho`（Zen Old Mincho） | 氏名・セクション見出し・社名・肩書き |
| `--font-gothic`（Zen Kaku Gothic New） | 本文・注釈・一覧 |

- **明朝は「名前を持つもの」に使う**（人名・会社名・セクション名）。説明文には使わない。
- 本文は 16px / `line-height: 1.8`（PC は 1.7）。`font-feature-settings: 'palt'` で和文の字間を詰めている。

### 趣味のモチーフ（旅行・サッカー）

独立セクションは作らず、**ビジュアルに溶かす**。気づかれなくてよい濃度に保つ。

- **旅**：経歴を「旅程」として組む。`.journey` の縦線＋朱の停留点がそれ。
- **サッカー**：`body` の背景に敷いた極薄の横ストライプ（芝の刈り跡）と、`.profile::before` のセンターサークル。
- **禁止**：ボール・ユニフォーム・飛行機・国旗などの直接的な図像。一気に素人っぽくなる。

---

## 2. 余白は 8px グリッドに拘束する

`--s1`(8) / `--s2`(16) / `--s3`(24) / `--s4`(32) / `--s5`(48) / `--s6`(64) の **6段だけ**を使う。

- `margin` / `padding` / `gap` に **6px や 13px のような中間値を書かない**。
- 過去に「1ドット=4px」と宣言しながら誰も守らず、全体が緩く見える状態になった。**宣言したグリッドは必ず守る。**
- フォントサイズはこの限りではない（13 / 15 / 16 / 17 / 18 / 24 / 28px を使用）。ただし新しいサイズを増やさない。

---

## 3. CSS の書き方（ここを破ると実際に壊れた）

### 3.1 メディアクエリは**必ずファイル末尾**にまとめる

```css
/* ❌ 基本ルールより前に置くと、後続の shorthand に打ち消される */
@media (min-width: 48rem) { h1 { margin-top: 8px; } }
h1 { margin: 16px 0 0; }   /* ← これが勝ってしまう */
```

メディアクエリは詳細度を上げない。**効くかどうかは純粋に記述順**で決まる。
`Portfolio.astro` では PC 向けの上書きを `<style>` の末尾に1ブロックだけ置いている。増やさない。

### 3.2 セクション間の余白は「見出し」ではなく「コンテナ」が持つ

`h2` に `margin-top` を持たせると、2段組にしたときにカードと見出しが密着して崩れる。
余白は `.sidebar > section` / `.content > section + section` 側で管理する。

### 3.3 枠線は `.pixel-frame` 的な box-shadow を各所にコピーしない

同じ装飾を2箇所以上に書きたくなったら、クラスに切り出して使い回す。

---

## 4. レイアウトの制約

- **PC（≥48rem）はビューポート内に収める。** 基準は **1366×768**。この幅でスクロールが出たら余白を詰める（フォントを縮めるより先に余白）。
- モバイルはスクロール前提。無理に詰めない。
- ブレークポイントは `48rem`（768px）の**1つだけ**。増やさない。
- PC は2段組（左：人物／右：実績）、モバイルは1段。

---

## 5. 多言語（JA / EN）

Astro 組み込みの i18n ルーティングは**使っていない**。素朴な別ページ方式。

```
src/data/content.ts        ← ja / en の文言をすべてここに集約
src/components/Portfolio.astro  ← lang を受け取って描画する共通コンポーネント
src/pages/index.astro      ← <Portfolio lang="ja" />
src/pages/en/index.astro   ← <Portfolio lang="en" />
```

- **文言をコンポーネントに直接書かない。** 必ず `content.ts` に足して両言語を埋める。
- `BaseLayout` に `lang` を渡すと `<html lang>` と `og:locale` が切り替わる。渡し忘れると日本語扱いになる。
- 英語版は直訳しない。職務経歴書の内容を要約して自然な英語にする。

### 固有名詞は翻訳せず、一次情報で裏を取る

社名・人名・製品名を**定型変換で英訳しない**。「株式会社◯◯ → ◯◯ Co., Ltd.」は高確率で誤る。

- 必ず**公式サイトの会社概要 / Company Profile / フッターの著作権表記**を確認する。
- 実例：`株式会社ジール` を `ZEAL Co., Ltd.` と書いていたが、公式表記は **`ZEAL Corporation`**（公式サイトのフッターが `©ZEAL Corporation.`、親会社 Avant Group の英語版グループ一覧も同表記）。`ZEAL Co., Ltd.` は公式のどこにも存在しない綴りだった。
- 確認できない場合は勝手に英訳せず、日本語表記のまま残すか、確認できていないことを明示する。

現在の確定表記:

| 日本語 | 英語 |
| --- | --- |
| 株式会社MIXI | `MIXI, Inc.` |
| 株式会社ジール | `ZEAL Corporation` |

---

## 6. 見た目の検証方法

ブラウザ拡張から `localhost` はブロックされることがある。**ヘッドレス Chrome で撮るのが確実**。

```bash
npx astro dev --host 127.0.0.1 --port 4321 &

CHROME="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
"$CHROME" --headless=new --disable-gpu --hide-scrollbars \
  --window-size=1366,768 --screenshot=/tmp/shot.png http://127.0.0.1:4321/
```

### 落とし穴

- **macOS のヘッドレス Chrome は最小ウィンドウ幅 500px に丸められる。**
  `--window-size=390,844` と指定しても実際は 500px 幅で描画され、画像だけが 390px に切り取られる。
  その結果「モバイルで横にはみ出している」ように見えるが、**それは計測の誤り**。狭い幅の確認は 500px を下限として行う。
- 画面下中央に出る小さなバーは **Astro の Dev ツールバー**。本番ビルドには出ない。
- CSS を直したのに反映されないときは dev サーバーが古い状態を掴んでいる。`npx astro dev stop` して起動し直す。

---

## 7. 変更後に必ず通すもの

```bash
npm run build   # astro check（型）＋ astro build（/ と /en/ の2ページ生成）
```

- 型エラー 0、ページ 2枚が生成されることを確認する。
- そのうえで 1366×768 と 500px 幅のスクリーンショットを撮り、**目で見て**崩れがないことを確認してから完了とする。
